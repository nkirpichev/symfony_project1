{% extends 'base.html.twig' %}

{% block title %}Hello ArtisanController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>Hello {{ controller_name }}! ✅</h1>

    Liste des artisans:
    <ul>
        {% for artisan in arrArtisan %} 
            <li>{{ artisan.nom ~ " " ~ artisan.prenom ~ " - " ~ artisan.metier}}</li>
        {% endfor %}
     </ul>
        Liste des stagiaires:
    <ul>
        {% for stag in arrStag %} 
            <li>{{ stag }}</li>
        {% else  %} Il n'y a pas de stagiaires
        {% endfor %}
     </ul>

</div>
{% endblock %}
